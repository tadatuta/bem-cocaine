this._mode === '' && !this.data: {
    return apply(
        this._mode = '',
        this.data = {
            bundleName : 'index',
            pageTitle  : 'Обработчик события click'
        },
        this.ctx = {
            block : 'page',
            mods  : this.ctx.err? { error : 'not-found' } : {}
        }
    );
}
