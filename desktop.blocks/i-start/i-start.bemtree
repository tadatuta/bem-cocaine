this._mode === '' && !this.data: {
    return apply(
        this._mode = '',
        this.data = {
            bundleName : 'index',
            pageTitle  : 'bem on cocaine'
        },
        this.ctx = {
            block : 'page',
            mods  : this.ctx.err? { error : 'not-found' } : {}
        }
    );
}
